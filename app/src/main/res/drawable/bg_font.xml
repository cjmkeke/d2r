<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Add padding to the whole rectangle -->
    <item>
        <shape android:shape="rectangle">
            <padding android:bottom="2dp" android:left="2dp" android:right="2dp" android:top="2dp" />
        </shape>
    </item>

    <!-- Add a solid color rectangle with rounded corners -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
            <corners android:radius="1dp" />
        </shape>
    </item>

    <!-- Add color to the corners -->
    <item android:left="0dp" android:right="0dp" android:top="0dp" android:bottom="0dp">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" /> <!-- Change the color to your desired color -->
            <corners android:radius="1dp" />
        </shape>
    </item>

    <!-- Add a border around the rectangle -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:color="#CFCFCF"
            android:width="1dp" />
            <corners android:radius="1dp" />
        </shape>
    </item>
</layer-list>
